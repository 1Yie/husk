[package]
name = "agent-plugin"
version = "0.1.0"
edition = "2021"
description = "Plugin system: manifest, Plugin trait, PluginManager, MCP stdio bridge (WASM behind feature)"
license = "MIT"

[dependencies]
agent-llm = { path = "../agent-llm" }   # ToolCall shape only
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
tokio.workspace = true
schemars.workspace = true
toml.workspace = true
libc = "0.2.189"

[dev-dependencies]
tempfile = "3"
